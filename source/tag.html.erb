<h1>Tag: <%= tag %></h1>

<% videos.each do |event, videos| %>
  <% videos.each do |video| %>
    <p> <%= link_to event, "/events/#{data.events[event].slug}" %> </p>
    <%= partial :video, :locals => { :video => video } %>
  <% end %>
<% end %>
